#include<iostream> 
#include<cstdio> 
#include<algorithm> 
#include<cstring> 
#include<string>
#include<cmath> 
#include<vector> 
#include<queue> 
#include<map>
#include<ctime>
#include<set>
typedef long long lld; 
using namespace std; 
#define debug(x) cout<<#x<<"="<<x<<endl;
#define here cout<<"_______________here "<<endl;
#define clr(NAME,VALUE) memset(NAME,VALUE,sizeof(NAME)) 
#define MAX 0x7f7f7f7f 
#define N 16000010
#define PRIME 999983

void hello()
{
    for (int j = 1000; j < 1020; j++) {
        printf("hello j %d\n", j);
    }
    
    printf("until will come here\n");


    for (int i = 10; i < 20; i++) {
        printf("now i is here\n");
    }
}
int main()
{
    int num;
    for (num = 0; num < 10000; num++) {
        printf("Now i is heihei %d\n", num);
    }
    hello();
    return 0;
}
